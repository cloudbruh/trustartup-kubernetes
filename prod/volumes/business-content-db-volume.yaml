apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: business-content-db-volume
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: default
